require 'spec_helper'

describe new_relic_rest_client do
  it 'has a version number' do
    expect(new_relic_rest_client::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
